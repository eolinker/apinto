package monitor_entry

var (
	LabelNode     = "node"
	LabelCluster  = "cluster"
	LabelApi      = "api"
	LabelApp      = "app"
	LabelHandler  = "handler"
	LabelUpstream = "upstream"
)

var labels = []string{
	LabelNode,
	LabelCluster,
	LabelApi,
	LabelApp,
	LabelHandler,
	LabelUpstream,
}
